@import "tailwindcss";
@plugin "daisyui" {
  themes: light --default, dark --prefersdark;
  logs: false;
}

/* Django templates live outside frontend/ — explicit source needed */
@source "../../templates/**/*.html";
